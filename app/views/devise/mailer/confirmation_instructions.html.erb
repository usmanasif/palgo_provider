<p>Welcome <%= @resource.email %>!</p>

<p>You can confirm your account email through the link below:</p>
<p><%= @resource.mobile_code %></p>
<p><%= link_to 'Confirm my account',  new_user_confirmation_url(@resource, :confirmation_token => @resource.confirmation_token) %></p>
